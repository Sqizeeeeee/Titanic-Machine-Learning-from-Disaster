# Epoch 5 — Random Forest on Titanic

## 🎯 Цель эпохи
Исследовать возможности деревьев решений и ансамблей Random Forest для Titanic dataset.  
Проверить, насколько ручной Random Forest способен улучшить результат по сравнению с одиночным деревом и логистической регрессией.

---

## 🛠 Что сделано
- Реализован **ручной Decision Tree (CART)** с Gini index и остановкой по `max_depth` и `min_samples_leaf`.
- Реализован **ручной Random Forest** с бутстрэппингом и ансамблированием нескольких деревьев.
- Добавлена **обёртка над sklearn Random Forest** для честного сравнения.
- Проведён **grid search ручного RF**:
  - `n_estimators`: 25, 50, 100
  - `max_depth`: 4, 6, 8
  - `min_samples_leaf`: 1, 3, 5
  - `max_features`: None / sqrt(n_features)


---

## 📊 Результаты

| Модель                     | Accuracy (val) | Kaggle Score |
|-----------------------------|----------------|--------------|
| Decision Tree (manual)      | 0.8045         | —            |
| Random Forest (manual)      | 0.8380         | 0.77990      |
| Random Forest (sklearn)     | 0.8212         | —            |

---

## Выводы

- **Ручной RF** дал ощутимый прирост по сравнению с одиночным деревом и логрегом.  
- **Feature subsampling** не дал улучшения — признаки сильные, мало и практически не коррелированы.  
- **Sklearn RF** на текущих параметрах немного уступает ручному RF на небольшом датасете.  
- **Потолок Epoch 5** достигнут: дальнейший прирост вероятно только с использованием бустинга или новых признаков.  

---

## 📝 Итоги
Epoch 5 показала, что Random Forest — реальный шаг вперед для Titanic, но потолок был достигнут быстро.
