# Epoch 6 — CatBoost Grid Search

## 🎯 Цель эпохи
Провести детальный подбор параметров для CatBoost на данных Titanic после корректного заполнения пропусков и бининга возраста.  
Цель — понять предел производительности одной модели CatBoost и получить стабильные базовые результаты для будущих улучшений.

---

## ✅ Что было сделано

1. **Подготовка данных**
   - Все пропуски в `Age` и `Age_group` заполнены с помощью KNN.
   - Категориальные признаки выделены для CatBoost (`Sex`, `Embarked`, `Title` и т.д.).
   - Пайплайн привёл данные в вид, подходящий для деревьев и CatBoost.

2. **CatBoostModel**
   - Реализована обёртка с параметрами:
     - `iterations`, `learning_rate`, `depth`, `l2_leaf_reg`, `bagging_temperature`, `rsm`.
   - Исправлено:
     - `eval_metric = "Logloss"` вместо `Accuracy`.
     - `use_best_model` для корректного early stopping.

3. **Grid Search**
Перебраны комбинации параметров:
    - `iterations = [200, 500]`
    - `learning_rate = [0.01, 0.03, 0.05]`
    - `depth = [3,4,5]`
    - `l2_leaf_reg = [1,3]`
    - `bagging_temperature = [0.0, 1.0]`
    - `rsm = [0.7, 1.0]`

4. **Оценка**
   - Accuracy на валидации использовалась для раннего выбора best_params.
   - Лучшая модель:
     ```python
     {'iterations': 200, 'learning_rate': 0.01, 'depth': 4,
      'l2_leaf_reg': 3, 'bagging_temperature': 0.0, 'rsm': 1.0,
      'val_accuracy': 0.8212}
     ```

---

## ⚡ Выводы

- CatBoost показал **CV ≈ 0.821** на валидации.
- Модель **стабильна, без стохастики** (`bagging_temperature = 0.0`).
- Early stopping сработал на 59 итерациях, дальнейшее увеличение `iterations` не улучшает результат.

---

## 🛠 Что не помогло

- Увеличение количества деревьев (`iterations`) сверх 200.
- Более агрессивный bagging / rsm.
- Изменение глубины или l2 без добавления новых признаков.
